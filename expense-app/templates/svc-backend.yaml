apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    project: expense-app
    tier: backend
    component: backend
  ports:
  - name: backend-port
    protocol: "TCP"
    port: 8080
    targetPort: 8080