apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  type: {{ .Values.svc-frontend.type }}
  selector:
    project: {{ .Values.labels.project }}
    tier: {{ index .Values.labels.tier 2 }}
    component: {{ index .Values.labels.component 2 }}
  ports:
  - name: frontend-port
    protocol: "TCP"
    targetPort: {{ .Values.svc-frontend.port }}
    port: {{ .Values.svc-frontend.port }}
    nodePort: {{ .Values.svc-frontend.nodePort }}